services:
  - type: web
    name: mlflow-server
    runtime: docker
    dockerfilePath: ./mlflow_server/Dockerfile
    disk:
      name: mlflow-data
      mountPath: /mlruns
      sizeGB: 1
    envVars:
      - key: PORT
        value: 8000